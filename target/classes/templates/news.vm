<html>
<body>
<pre>
    Hello VM

    value1:$!{value1}
    value2:$!{value2}
    value3:${value3}

    #foreach($color in $colors)
        #render_color($color, $foreach.index)
    #end

    $!{user.name}
    $!{user.getName()}

    #set($tiltle = 'myproject')
    Include:#include("header.vm")  <br>
    Parse:#parse("header.vm")

    #foreach($color in $colors)
        #render_color($color, $foreach.index)
    #end

    #macro(render_color, $color, $index)
        Color by Macro $color, $index
    #end


</pre>
</body>
</html>